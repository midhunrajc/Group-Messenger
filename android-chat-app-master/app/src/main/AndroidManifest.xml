<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.meporio.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">
        <activity
            android:name="com.meporio.app.MainActivity"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name="com.meporio.app.LoginActivity"
            android:theme="@style/Theme.AppCompat.Light" />
        <activity
            android:name="com.meporio.app.ChatActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="com.meporio.app.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <service android:name="com.meporio.app.Service.MyFirebaseMessagingService"
            android:exported="true"/>
    </application>

</manifest>